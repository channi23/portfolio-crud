# Creating of Portfolio-crud application with svelte kit (so that i can learn)

so before i start writing the code, one of the first things that i understood about svelte and svelte kit is, that svelte is basically used for the frotend and svelte kit is more of build upon svelte to also include the functionalities of full-stack application framework (like SSR(server side rendering) and a lot more)

so for now i basically understood that frontend also have the same ideology of delegating responsibilities, like in svelte which is basically for frontend we have page, components and types, and one more part is we do not need to actually do anything for the routing in here, just go into the routing dir in the src and just always add file like +page.svelte under any dir named with the respectiev pages in the frontend.


so i would like to move in this way, while i write the code

types->storage->components->page

basically bottom-up, if i am not wrong we start with smaller pieces to make the full page

